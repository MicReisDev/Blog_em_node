
<h1>Editar</h1>
<div class="card">
  <div class="card-body">
    <form action="/admin/categorias/editado" method="post">
    <input type="hidden" name="id" id="id" value="{{categorias._id}}">
      <label for="nome">Nome:</label>
      <input type="text" name="nome" id="nome" placeholder="Digite o nome da categoria" class="form-control" value = "{{categorias.nome}}">
      <label for="slug">Slug:</label>
      <input type="text" name="slug" id="slug" placeholder="Conteúdo da categoria" class="form-control" value = "{{categorias.slug}}">
      <button class="btn btn-primary mt-2">Salvar edição</button>

    </form>
  </div>
</div>